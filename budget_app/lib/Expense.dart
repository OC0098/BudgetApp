
import 'package:flutter/material.dart';

class ModifyExpensesScreen extends StatelessWidget {
  const ModifyExpensesScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Modificar Gastos")),
      body: const Center(child: Text("Pantalla para Modificar Gastos")),
    );
  }
}